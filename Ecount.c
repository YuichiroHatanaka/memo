#include<stdio.h>
#include <ctype.h>

char a;
int x, i;
int c;
int count[26];
FILE *lf;

int main(void){
  fopen("test.txt","r");
  while((c=fgetc(lf))!=EOF){
    count[c-65];
  }
  for(
  printf("%c はテキスト内に %d 個ある。",a ,x);
}
